export const DEFAULT_PAGE_TITLE = 'PAGE_TITLE.DEFAULT';

export const ENVIRONMENT = import.meta.env.VITE_ENVIRONMENT;

export const SUPPORTED_LANGUAGES = ['en', 'hu'] as const; // Fallback language must be first item
